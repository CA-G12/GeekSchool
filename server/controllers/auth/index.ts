import logout from './logout';
import userData from './userData';
import login from './login';

// it will must be change to export {} when we add more controllers like signIn or signUp
export { logout, userData, login };
